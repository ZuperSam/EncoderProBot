apiVersion: v1
kind: Service
metadata:
    name: "kmac-91c7"
    labels:
        app: "kmac-91c7"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-91c7"